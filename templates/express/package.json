{
  "name": "{{project-name}}",
  "version": "{{project-version}}",
  "description": "{{project-description}}",
  "scripts": {
    "prepare": "tsc",
    "start": "node ./build/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "@pomgui/rest": "^0.1.0-beta.0",
    "@pomgui/database": "^0.1.0-beta.0",
    "sitka": "^1.0.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.1",
    "@types/node": "^12.7.5",
    "typescript": "^3.6.3"
  }
}
